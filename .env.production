# Hosted build: the SDK talks to the managed endpoint (cobrowse.unikernel.ai) by
# default, so only the tenant is named here. Never point a hosted build at a local
# worker — .env.development.local does that, and Vite loads it in dev only.
VITE_COBROWSE_TENANT=perfios
